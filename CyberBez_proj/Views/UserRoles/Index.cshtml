@using CyberBez_proj.Models
@model List<CyberBez_proj.Models.UserRolesViewModel>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h1>User List</h1>
<a class="btn btn-primary" asp-area="Identity" asp-page="/Account/Register">Add user</a>
<table class="table table-striped">
    <thead>
        <tr>
            <th>UserName</th>
            <th>Email</th>
            <th>Roles</th>
            <th>Action</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.UserName</td>
                <td>@user.Email</td>
                <td>@string.Join(" , ", user.Roles.ToList())</td>
                <td>
                    <a class="btn btn-primary" asp-controller="UserRoles" asp-action="Manage" asp-route-userId="@user.UserID">Manage Roles</a>
                </td>
                <td>
                    <form method="post" asp-action="Delete" asp-route-userId="@user.UserID">
                        <button class="btn btn-danger" type="submit">Delete</button>
                    </form>
                </td>
                @*<td>
                    <form method="post" asp-action="Lock" asp-route-userId="@user.UserID">
                        <button class="btn btn-danger" type="submit">Lock</button>
                    </form>
                </td>
                <td>
                    <form method="post" asp-action="Unlock" asp-route-userId="@user.UserID">
                        <button class="btn btn-primary" type="submit">Unlock</button>
                    </form>
                </td>*@
            </tr>
        }
    </tbody>
</table>